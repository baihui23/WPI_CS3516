# Project 3: Implement a distributed asynchronous distance vector (DV) routing protocol

1. First compile the the program
    Run make from terminal

2. Run the program with the following usage
    USAGE: ./project3 1 where 1 is the trace level